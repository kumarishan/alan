subprojects {
  apply plugin: 'java'

  description = 'A distributed, stateless and persistent state machine'
  version = '0.1'

  repositories {
      mavenCentral()
  }

  dependencies {
    testCompile 'junit:junit:4.12'
  }

  jar {
    manifest.attributes provider: 'gradle'
  }
}
